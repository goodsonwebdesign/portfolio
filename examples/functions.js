


//var stands for variable
//sampleFuntion is the name of the function and is how the function is called into action later 
//The word function is telling the code that we are making a specific type of var that behaves like a funtion
//the parenthesis are were we put our parameters which is what we pass into the function for it to use
//The curly/mustache brackets denote the body of the function which is what runs as the funtion is called and uses the set parameters that are passed in
var sampleFunction = function () {
    console.log('this is a function declaration');
}



var sampleFunctionWithParameters = function (sampleparameter1, sampleparameter2) {
    console.log(sampleparameter1);
    console.log(sampleparameter2);
}